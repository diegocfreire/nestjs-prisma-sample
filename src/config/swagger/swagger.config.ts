import { SwaggerConfig } from './swagger.interface'

export const SWAGGER_CONFIG: SwaggerConfig = {
  title: 'NestJS Sample Application With PrismaORM',
  description: 'NestJS Sample Application With PrismaORM.',
  version: '1.0',
  tags: ['*Auth', 'Users'],
}
